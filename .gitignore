# AL / Business Central build artifacts
# Compiled packages should not land in the repo root — see /releases for tracked release builds.
*.app
!/releases/*.app

# Symbol cache for dependencies (regenerate with "AL: Download Symbols")
.alpackages/

# Snapshot debugger session state and rapid application development state
.snapshots/
.vscode/rad.json

# Agentes de desarrollo (definiciones y memoria): viven solo en
# https://github.com/cristhianlopez14/AGENTES-DESARROLLO-BC, no en este repo
.claude/agents/
.claude/agent-memory/
.claude/agentes-desarrollo-bc/
